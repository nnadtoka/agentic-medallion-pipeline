[pytest]
markers =
    eval: behavior eval against the live mcp-server (requires `docker compose up -d mcp-server` + seeded templates). Self-skipping if the server isn't reachable -- see pipeline/tests/mcp_agent/test_search_evals.py.
